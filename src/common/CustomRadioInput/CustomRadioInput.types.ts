export type FormData = {
  projectName: string;
  projectURL: string;
  projectCategory: string;
  projectDetails: string;
  workers: number;
  productLaunch: string;
  email: string;
};

export type CustomRadioInputData = {
  selectedItem: string;
  fieldName: string;
  index: number;
  option: string;
};
